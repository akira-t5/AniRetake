# Changelog

このファイルは AniRetake の変更履歴です。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に近い記法です。

## [1.5.0] - 2026-09-03

### 追加
- 画像の自動保存先をIndexedDBに分離（容量の目安がlocalStorage約5MBから数百MB級に拡大）。テキストのみlocalStorageに保存するため、画像が多い状態での入力中の自動保存も軽量化（実測0.3ms）
- 画像登録時の自動最適化：長辺1920pxに縮小し、透過のない画像はJPEG(q0.85)に変換して1/5〜1/15に削減。小さい画像・透過PNGは元のまま保持

### 互換性
- JSON保存・読込の形式は従来どおり（画像埋め込み）を維持。既存のlocalStorageデータは初回起動時に自動でIndexedDBへ移行

## [1.4.5] - 2026-09-03

### 修正
- TSV出力でリテイク内容の改行（段落）が空白に潰れていたのを修正。改行・タブ・引用符を含むセルをスプレッドシート標準の引用符形式で出力し、Excel/スプレッドシート貼り付け時にセル内改行が保持されるようにした

## [1.4.4] - 2026-08-28

### 修正
- HEIC等のブラウザで表示できない画像を登録しようとした場合、登録をスキップして理由を警告するように（これまで登録だけされて表示されない状態になっていた）
- JSON読込時に表示できない画像が含まれている場合、枚数と対処法を警告するように（データ自体は保持される）

## [1.4.3] - 2026-08-28

### 修正
- 画像の読み込み（FileReader）が完了する前にJSON保存・PNG書き出しを実行すると画像が欠ける競合を修正（読込完了を待ってから実行）

## [1.4.2] - 2026-08-28

### 追加
- JSON読込の完了表示に「Nカット・画像M枚」を明示（ファイルに画像が含まれるか即座に判別可能に）
- 画像データがブラウザの保存容量を超え自動保存できない場合、リロードで消える旨をアラートで明示
- JSON保存に作業中のカット位置を含め、読込時に同じカットで再開

### 修正
- 同じJSONファイルを続けて読み込めない問題を修正（読込後にファイル入力をリセット）

## [1.4.1] - 2026-08-28

### 修正
- 画像を含む大きなJSON保存でダウンロードが失敗しうる問題を修正（Object URLの即時破棄を遅延化）
- JSON保存ボタンに「画像データを含む」旨の補足を追加（画像を含む全データが保存され、別環境でそのまま再開できることを実機で検証済み）

## [1.4.0] - 2026-08-28

### 追加
- 全PNG書き出しをZIPファイルにまとめて出力（`retake_png_作品_話数_R-n.zip`）
- プレビュー画面への画像ドラッグ&ドロップ登録
- 参考画像の順序入れ替え（サムネイルの◀▶ボタン）
- 表計算ソフト貼り付け用TSV出力（A列:通し番号 B列:カット番号 C列:リテイク内容 D列:兼用カット番号、UTF-8 BOM付き。セル内改行は空白に置換）

### 変更
- 左サイドバーのアプリ名・出力・保存エリアを固定表示（カット一覧をスクロールしても消えない）
- 出力・保存エリアを見出しクリックで開閉可能に
- 画像1枚時は画像を下側に配置し、上部スペースをメモ用に開放
- 画像0枚時のPNG出力から「画像エリア」の枠・表記を省略（メモ欄として使用可能）

### 修正（パフォーマンス）
- 画像登録後、キー入力のたびに画像要素を再生成・再デコードしていた問題を解消（画像に変更がない限り再描画をスキップ）。画像が多い・大きいほど効果が大きい

## [1.3.3] - 2026-08-28

### 修正
- 改行を入力すると文字数カウントの上限が崩れる不具合を修正（本文欄では上限が31行分に激減、備考欄では10000に激増していた）。上限実測のプローブ文字に改行を使わず、直前の通常文字で測るようにした
- 備考欄のプレビューで改行が潰れて表示されていたのを、PNG出力と同じく改行が反映されるようにした

## [1.3.2] - 2026-08-26

### 修正
- 全体的な動作の遅さを改善。キー入力のたびに文字数上限を DOM 実測で再計算していたのを、枠サイズ・文字ごとのキャッシュ化＋次フレームへの集約に変更し、フォント自動縮小も1pxずつのループから二分探索にした（実機Chrome計測：1キーあたり中央値 120〜137ms → 約1ms、PNG書き出し 459ms → 45ms）

## [1.3.1] - 2026-08-25

### 修正
- localStorage が使えない環境（容量超過・ブロック等）で自動保存が失敗したとき、従来は通知なく止まっていたのを「自動保存：保存できません」と赤表示するようにした
- 最後の1件のカットを削除しようとしたとき、無反応だったのを「最後の1件は削除できません」と案内するようにした
- ヘルプの「変更履歴」を GitHub 上の CHANGELOG への URL 表記にした

## [1.3.0] - 2026-08-25

### 追加
- 環境設定で書き出し用紙サイズを選択（A5横 2480×1748 / A4横 3508×2480、いずれも300DPI）
- 環境設定でショートカットキーを変更可能（初期値に戻すも可）

## [1.2.2] - 2026-08-25

### 修正
- 文字数上限をプレビュー枠の実測に合わせた（親枠に収まるまでフォント縮小してからカウント）

## [1.2.0] - 2026-08-25

### 追加
- リテイク内容／備考のプレビュー表示上限をカウントダウン表示（超過時はマイナス・赤字、入力は継続可）

### 変更
- 「リテイク内容と処理指示」→「リテイク内容　修正指示」に文言変更

## [1.1.0] - 2026-08-25

### 追加
- ヘルプ／バージョン情報画面（免責事項・ライセンス・サポート・ショートカット一覧）
- `CHANGELOG.md`（公開用の変更履歴）
- PNG書き出し前の必須入力チェック
- 全PNG書き出し時の進捗表示
- JSON読込失敗時のわかりやすいエラー表示

### 変更
- バージョン番号を `APP_VERSION` で一元管理

### 修正
- R回数が1桁のとき消して書き換えられない問題

## [1.0.1] - 2026-08-25

### 追加
- 初回公開（単一HTMLのリテイク指示カード作成ツール）
